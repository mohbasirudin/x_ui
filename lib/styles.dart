import 'package:flutter/material.dart';

class StyleText {
  static TextStyle btn({
    required var color,
    required var fontSize,
  }) {
    return TextStyle(
      color: color,
      fontSize: fontSize,
    );
  }
}
